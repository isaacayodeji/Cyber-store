import { appTitle } from "../../utils/helper";

const SingleProduct: React.FC = () => {
      document.title = `Product${appTitle}`;

  return <div>SingleProduct</div>;
};
export default SingleProduct;
